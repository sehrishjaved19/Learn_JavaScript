
# Debugging Example

A simple JavaScript project that demonstrates how to use the **`debugger`** statement to inspect and fix code.

## 📂 Files
- `index.html` — Main structure and input fields  
- `debug_code.js` — Logic and debugging example  
- `debug_code.css` — Page styling

## ⚙️ How It Works
1. Enter two numbers.  
2. Click **"Perform Operation"**.  
3. The `multiply()` function runs and pauses at the `debugger;` line.  
4. Open **Developer Tools → Sources** to inspect variables.

## 💡 Concepts
- DOM input/output  
- `parseInt()` and `isNaN()`  
- `debugger` usage  
- Function and event handling
